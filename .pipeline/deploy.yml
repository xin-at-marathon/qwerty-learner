version: 0.2

phases:
  build:
    commands:
      - echo started on `date`
      - BUILD_ID=`cut -d ":" -f2 <<< $JOB_BUILD_ID` aws s3 cp s3://artifact-bucket-devops-xincloud/codebuild/$BUILD_ID/output.zip output.zip
      - unzip output.zip
      - make deploy
